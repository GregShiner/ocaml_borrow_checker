((check this (shit out))
    #;(comments work
        on (datums too))

    #| Block comments! |# (bruh) #| nested #| ones too |# |#

    ; boring ahh line comments
    (and they can) ; go at the end of lines too
)
